DATABASE_URL=postgres://postgres:password@localhost/h1r0_development
BIND_ADDR=0.0.0.0:3000
# Set PROJECT_ROOT to the absolute path of the H1.R0 repo root when running
# the compiled binary from outside the project directory
PROJECT_ROOT=/path/to/H1.R0
IMAGES_BASE_PATH=/path/to/H1.R0/app/assets/images
