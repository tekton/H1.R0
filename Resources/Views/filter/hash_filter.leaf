#extend("base"):
#export("content"):
<div class="hash_data_top">
    |
    #for(t in pairs):
        #(t.tag) :: #(t.value) |
    #endfor
</div>

<div class="exif_table">
    <ul id="quad">
        #for(image in images):
            <li><a href="/images/#(image.id)"><img src="/assets/images/thumbnails/#(image.location)" class="browse_top_block_image"></a></li>
        #endfor
    </ul>

    <div id="tags_list">
        <ul id="quad">
            #for(exif_data in refined):
                <li><a href="/filter/#(exif_data.q)">#(exif_data.tag):#(exif_data.value) (#(exif_data.count))</a></li>
            #endfor
        </ul>
        <div class="both"></div>
    </div>
</div>
#endexport
#endextend
