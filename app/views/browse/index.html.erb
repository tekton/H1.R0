<% if notice %>
	<p id="notice"> <%= notice %> </p>
<%end %>

<div id="top_block">
	<table>
		<tr>
		<% @images.each do |image|%>
	    	<td><%= link_to image_tag(asset_path('thumbnails/'+image.location), class: 'browse_top_block_image'), image %></td>
		<% end %>
		</tr>
	</table>
</div>
